# Review Notes

Exported review-note packets from the screenplay tool can be stored here.

Packets are JSON files that include note text, target shot metadata, related shots,
and base64-encoded attachments. The GitHub Pages page cannot write to this repo by
itself, so download a packet from the Review Archive panel and commit it here when
notes need to travel with the repo or be reviewed by an agent.
